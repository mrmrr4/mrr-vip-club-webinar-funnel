{
  "buildCommand": "cat site.b64.* | base64 -d > site.tar.gz && rm -rf public && mkdir -p public && tar -xzf site.tar.gz -C public",
  "outputDirectory": "public",
  "framework": null,
  "cleanUrls": true
}